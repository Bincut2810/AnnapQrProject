namespace Annap.CoffeeQrOrdering.Application;

/// <summary>Quiet alternative direction from the same menu grounding set.</summary>
public sealed record SommelierAlternativeCup(Guid? MenuItemId, string Name, string? Note);
